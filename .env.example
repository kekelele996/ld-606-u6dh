# 复制为 .env 后由 docker compose 读取：cp .env.example .env && docker compose up -d
COMPOSE_PROJECT_NAME=port-yard
FRONTEND_PORT=20106
BACKEND_PORT=21106
DB_PORT=33060
DB_NAME=app_db
DB_USER=app_user
DB_PASSWORD=app_password
JWT_SECRET=local-dev-secret
